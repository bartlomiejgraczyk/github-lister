spring:
    application:
        name: github-lister-discovery-server
    profiles:
        active: dev
        
    cloud:
        config:
            uri: http://localhost:8888
            enabled: true